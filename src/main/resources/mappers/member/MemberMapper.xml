<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="daonplace.com.jeonnambackpacking.mapper.MemberMapper">
    <insert id="insertMember" parameterType="Member" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Member (email, nickname, password, grade, sign_up_datetime, info_up_datetime)
        VALUES (#{email}, #{nickname}, #{password}, #{grade}, #{signUpDate}, #{infoUpDate})
    </insert>

    <update id="updateMember" parameterType="Member">
        UPDATE Member
        SET password = #{password}, nickname = #{nickname}, info_up_datetime = #{infoUpDate}
        WHERE email = #{email}
    </update>
    
    <select id="findByEmail" parameterType="string" resultType="Member">
        select * from Member
        where email = #{email}
        limit 1
    </select>

    <select id="exists" parameterType="object" resultType="boolean">
        select COUNT(*) > 0
        FROM Member

        <if test="email != null">
            WHERE email = #{email}
        </if>

        <if test="nickname != null">
            WHERE nickname = #{nickname}
        </if>

        limit 1
    </select>
</mapper>